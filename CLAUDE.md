# AGENTS.md

本仓的 agent 指令面只有一份：派给你的任务书 `docs/briefs/D<N>-impl.md` 或 `docs/briefs/D<N>-review.md`（先读它指的议题）。协调席规则见 `docs/agents/coordinator.md`。验证入口 `npm run check`。

上游（chuspeeism/dashi-taskboard）的 Taskboard Delivery Workflow 已移除：不认领、不走 taskctl 状态流转、不按 E3 自行扩展范围。
